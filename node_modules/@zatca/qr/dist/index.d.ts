export declare type EInvoiceFields = {
    sellerName: string;
    vatNumber: string;
    timestamp: string;
    total: string;
    vatTotal: string;
};
/**
 * Generates data string to generate qr from
 * Usage
 * ```js
 * const qrData = createQRData({
 *  sellerName: '',
 *  vatNumber: '',
 *  timestamp: '',
 *  total: '100.00',
 *  vatTotal: '15.00'
 * })
 * ```
 */
export declare function createQRData({ sellerName, vatNumber, timestamp, total, vatTotal, }: EInvoiceFields): string;
declare type QROptions = {
    format: "buffer" | "file" | "data-url" | "svg";
    filePath?: string;
};
export declare function generateQR(payload: EInvoiceFields, options?: QROptions): Promise<any>;
export {};
